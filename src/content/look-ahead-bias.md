# 做回测时踩过的第一个坑：look-ahead bias

很多看起来“收益很好”的策略，最后都死在一个问题上：用了未来数据。

## 什么是 look-ahead bias

在生成 t 时刻信号时，不小心使用了 t+1 才知道的信息。

## 我是怎么踩坑的

- 指标对齐错位
- 回测引擎默认 merge 方式错误
- 训练集和验证集切分不严谨

## 修复方式

1. 所有特征统一 `shift(1)`
2. 强制按时间滚动窗口切分
3. 回测结果与线上模拟环境对齐验证